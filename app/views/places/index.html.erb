<h1>Listing places</h1>
 
<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
  </tr>
 
  <% @places.each do |place| %>
    <tr>
      <td><%= place.name %></td>
      <td><%= place.description %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New place', new_place_path %>